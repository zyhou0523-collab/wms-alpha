# Demo Guide

## 一、登录系统

1. 打开 `http://localhost:5173`
2. 使用 `admin / admin123` 登录
3. 进入 WMS Alpha 首页

## 二、查看预期到货通知单

菜单路径：`入库管理 / 预期到货通知单`

演示点：

- 列表按入库单主表维度展示
- 每张入库单可展开查看产品行明细
- 状态、数量、SAP 回传状态集中展示

## 三、新建预期到货通知单

点击 `新建`，选择货主、仓库、订单类型和产品明细。

演示点：

- 产品按货主维度选择
- SN 管理产品和非 SN 管理产品共存
- 产品行可维护 SAP 工厂和库存地点

## 四、展开订单行明细

点击列表行左侧展开按钮。

演示点：

- 子表展示行号、产品编码、产品名称、SN 管理、计划数量、已采集、待收货、已收货
- 子表操作列不固定，展示更紧凑

## 五、SN 管理产品采集 SN

在主表或行明细中点击 `采集 SN`。

演示点：

- 订单维度采集窗口可选择产品明细行
- 切换行号自动带出产品信息和可采集数量
- SN 管理产品必须先采集再收货

## 六、非 SN 管理产品直接收货

非 SN 管理产品行不显示 `采集 SN`，可直接点击 `收货`。

演示点：

- 非 SN 产品无需采集 SN
- 收货数量不能超过计划数量

## 七、收货确认

点击 `收货`，选择目标库位并确认。

演示点：

- 已采集 SN 进入待收货
- 收货后更新已收数量
- 写入操作日志和收货批次记录

## 八、SAP 回传

收货完成后点击 `SAP 回传`。

演示点：

- 按收货批次回传 SAP Mock
- 成功后展示 SAP 凭证号
- 失败时写入接口日志

## 九、SAP 重传

勾选需要重传的入库单，点击 `重传 SAP`。

演示点：

- 支持失败/未回传单据重传
- 回传过程可在接口日志中查看

## 十、产品主数据导入导出

菜单路径：`基础数据 / 产品主数据`

演示点：

- 下载导入模板
- CSV 导入产品
- 按当前查询条件导出
- 同一货主下产品编码唯一

## 十一、客户主数据导入导出

菜单路径：`基础数据 / 客户主数据`

演示点：

- 客户 / 供应商 / 货主类型统一维护
- 下载模板、导入、导出
- 编码唯一，重复编码导入时更新

## 十二、SN 绑定列表导出

菜单路径：`入库管理 / SN 绑定`

演示点：

- 查看 SN、箱码、托盘码、ASN 单号
- 删除未收货的错误绑定关系
- 按当前查询条件导出 SN 绑定数据
